Create necessary python environment with

```conda create -c conda-forge -n snake python=3.8 numpy jupyter tensorflow tensorboard pandas matplotlib```

Also install
  - https://github.com/grantsrb/Gym-Snake
  - pip install gym